# 開発への参加

恒久ブランチは `main` だけです。forkまたは一時ブランチから `main` 向けに変更提案を送ってください。

授業の実データや個人情報をIssue、PR、テストへ含めないでください。変更理由、確認結果、第三者コードの出典とライセンスを記載してください。

ビルドとローカル起動はREADMEを参照してください。生成物を変更する場合は、対応する編集可能なソースも同時に更新します。
